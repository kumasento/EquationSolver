
#include <iostream>
#include <string>
#include <cmath>

#include "foo.h"

// use sqrt functions from cmath library
float s_sqrt(float x) { return sqrt(x); }
double d_sqrt(double x) { return sqrt(x); }
